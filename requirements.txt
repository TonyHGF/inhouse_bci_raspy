# Create/activate your own conda environment with Python 3.10 or 3.11 first.
# No environment creation or installation is performed by this project.
# Install a CUDA-enabled torch build separately if needed; pip keeps a
# preinstalled torch version that satisfies the constraint below.
mne==1.8.0
mne-bids==0.15.0
mne-bids-pipeline==1.8.0
numpy==2.0.2
scipy==1.13.1
pandas==2.3.3
matplotlib==3.9.4
scikit-learn==1.6.1
joblib==1.5.3
python-picard==0.8.2
pybv==0.7.6
torch>=2.5,<3.0
h5py>=3.11,<4
PyYAML>=6.0,<7
tensorboard>=2.17,<3
tqdm>=4.64,<5
